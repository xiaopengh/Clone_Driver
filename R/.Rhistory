shiny::runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
shiny::runApp()
runApp()
runApp()
runApp()
runApp()
if (!requireNamespace("BiocManager", quietly = TRUE))
install.packages("BiocManager")
BiocManager::install("GenomicRanges")
BiocManager::install("rtracklayer") # For reading GTF files
BiocManager::install("GenomicRanges")
library(GenomicRanges)
library(rtracklayer)
# 1. Read the BED file of sequenced regions
exome_regions <- import("../data/gencode.v19.basic.exome.bed")
# 2. Read the GTF file of gene annotations
gene_annotations <- import("../data/gencode.v19.annotation.gtf")
# 2. Read the GTF file of gene annotations
gene_annotations <- import("../data/gencode.v19.annotation.gtf")
# 2. Read the GTF file of gene annotations
gene_annotations <- import("../data/gencode.v19.annotation.gtf_withproteinids")
# 2. Read the GTF file of gene annotations
gene_annotations <- import("../data/gencode.v19.annotation.gtf")
# 2. Read the GTF file of gene annotations
gene_annotations <- import("../data/gencode.v19.annotation.gtf")
# Filter for only gene features
genes <- gene_annotations[gene_annotations$type == "gene"]
# 3. Perform the intersection
overlapping_genes <- intersect(genes, exome_regions)
# 3. Perform the intersection
overlapping_genes <- intersect(genes, exome_regions)
help(genes)
help(import)
showClass("GRanges")
help(GRanges::intersect)
methods(intersect)
head(genes)
names(genes)
columns(genes)
colnames(genes)
colnames(genes)
str(genes)
summary(genes)
dim(genes)
length(genes)
names(mcols(genes)) # Use mcols() to access metadata columns
seqinfo(genes)
R --version
version
library(GenomicRanges)
library(rtracklayer)
library(ggplot2)
library(dplyr)
library(tidyr)
library(ggpubr)
library(circlize)
library(ggbio)
library(GenomeInfoDb)
library(RColorBrewer)
library(viridis)
# 1. Read the BED file of sequenced regions
exome_regions <- import("../data/gencode.v19.basic.exome.bed")
# 2. Read the GTF file of gene annotations
gene_annotations <- import("../data/gencode.v19.annotation.gtf")
# Prepare the genomic data
genes_gr <- genes
seqlevelsStyle(genes_gr) <- "UCSC"  # Convert to UCSC style (e.g., "chr1")
# Calculate gene density per chromosome
chr_density <- data.frame(
chromosome = seqnames(genes_gr),
position = start(genes_gr),
gene_type = genes_gr$gene_type,
strand = as.character(strand(genes_gr))
) %>%
group_by(chromosome) %>%
mutate(
position_scaled = scale(position),
bin = cut(position, breaks = 100)
)
chromosome_density <- ggplot(chr_density) +
geom_density(aes(x = position, fill = gene_type, color = gene_type),
alpha = 0.3) +
facet_wrap(~chromosome, scales = "free_x", ncol = 3) +
scale_color_brewer(palette = "Set1") +
scale_fill_brewer(palette = "Set1") +
theme_minimal() +
theme(
strip.text = element_text(size = 14, face = "bold"),
axis.text.x = element_text(angle = 45, hjust = 1, size = 12),
axis.text.y = element_text(size = 12),
axis.title = element_text(size = 14),
legend.text = element_text(size = 12),
legend.title = element_text(size = 14),
plot.title = element_text(size = 16, face = "bold"),
panel.spacing = unit(2, "cm"),
aspect.ratio = 1  # This makes each facet square-shaped
) +
labs(
title = "Chromosome-specific Gene Density Patterns",
x = "Genomic Position",
y = "Density",
color = "Gene Type",
fill = "Gene Type"
)
chromosome_density
# Main visualization with multiple panels
scientific_plot <- ggplot(chr_density) +
# Panel A: Chromosome-wise gene density
geom_violin(aes(x = as.factor(chromosome), y = position),
fill = "lightblue", alpha = 0.6) +
facet_grid(. ~ "A: Gene Density Distribution") +
# Panel B: Gene type distribution
geom_bar(aes(x = as.factor(chromosome), fill = gene_type),
position = "fill") +
facet_grid(. ~ "B: Gene Type Composition") +
# Panel C: Strand bias
geom_bar(aes(x = as.factor(chromosome), fill = strand),
position = "fill") +
facet_grid(. ~ "C: Strand Distribution") +
# Common theme elements
theme_minimal() +
theme(
axis.text.x = element_text(angle = 45, hjust = 1),
panel.grid.major = element_line(color = "gray90"),
panel.grid.minor = element_blank(),
strip.text = element_text(size = 12, face = "bold"),
legend.position = "right"
) +
scale_fill_viridis_d() +
labs(
title = "Multi-dimensional Analysis of Gene Distribution",
x = "Chromosome",
y = "Position/Proportion",
fill = "Category"
)
scientific_plot
# Create statistical summary visualization
gene_stats <- chr_density %>%
group_by(chromosome) %>%
summarise(
gene_count = n(),
type_diversity = n_distinct(gene_type),
strand_ratio = sum(strand == "+") / n()
)
stats_plot <- ggplot(gene_stats) +
geom_point(aes(x = gene_count, y = type_diversity,
size = strand_ratio, color = chromosome)) +
scale_size_continuous(range = c(3, 10)) +
theme_minimal() +
labs(
title = "Chromosome Characteristics Overview",
x = "Number of Genes",
y = "Gene Type Diversity",
size = "Strand Ratio (+/-)",
color = "Chromosome"
)
stats_plot
# Generate numerical summaries
summary_stats <- chr_density %>%
group_by(chromosome) %>%
summarise(
n_genes = n(),
n_types = n_distinct(gene_type),
density = n() / (max(position) - min(position)),
strand_balance = sum(strand == "+") / n()
) %>%
arrange(desc(n_genes))
# Print summary statistics
print(summary_stats)
# Print summary information
cat("\nGene Annotation Summary:\n")
cat("------------------------\n")
cat("Total number of genes:", length(genes), "\n")
cat("Number of unique gene types:", length(unique(genes$gene_type)), "\n")
cat("Number of chromosomes:", length(unique(seqnames(genes))), "\n")
cat("Mean gene length:", mean(width(genes)), "bp\n")
cat("Median gene length:", median(width(genes)), "bp\n")
# Main visualization with multiple panels
scientific_plot <- ggplot(chr_density) +
# Panel A: Chromosome-wise gene density
geom_violin(aes(x = as.factor(chromosome), y = position),
fill = "lightblue", alpha = 0.6) +
facet_grid(. ~ "A: Gene Density Distribution") +
# Panel B: Gene type distribution
geom_bar(aes(x = as.factor(chromosome), fill = gene_type),
position = "fill") +
facet_grid(. ~ "B: Gene Type Composition") +
# Panel C: Strand bias
geom_bar(aes(x = as.factor(chromosome), fill = strand),
position = "fill") +
facet_grid(. ~ "C: Strand Distribution") +
# Common theme elements
theme_minimal() +
theme(
axis.text.x = element_text(angle = 45, hjust = 1),
panel.grid.major = element_line(color = "gray90"),
panel.grid.minor = element_blank(),
strip.text = element_text(size = 12, face = "bold"),
legend.position = "right"
) +
scale_fill_viridis_d() +
labs(
title = "Multi-dimensional Analysis of Gene Distribution",
x = "Chromosome",
y = "Position/Proportion",
fill = "Category"
)
scientific_plot
# 7. Create a genomic coverage plot for a subset of chromosomes
# Select first 5 chromosomes for visualization
top_chromosomes <- names(sort(table(seqnames(genes)), decreasing = TRUE))
genes_subset <- genes[seqnames(genes) %in% top_chromosomes]
coverage_plot <- ggplot(as.data.frame(genes_subset),
aes(x = start, y = seqnames, color = strand)) +
geom_segment(aes(xend = end, yend = seqnames), linewidth = 5) +
theme_minimal() +
labs(title = "Gene Coverage on Human Chromosomes",
x = "Genomic Position",
y = "Chromosome")
coverage_plot
summary(genes)
head(gene_annotations)
head(exome_regions)
gr1 <- GRanges(c("1", "chr1"), IRanges(c(1, 20), c(10, 30)))
gr2 <- GRanges(c("chr1", "chr1"), IRanges(c(5, 25), c(6, 26)))
gr1
gr2
ov <- findOverlaps(gr1, gr2)
ov
exome_regions
exome_regions$seqnames
summary(genes)
names(mcols(gene_annotations))
gr1 <- GRanges(c("chr1", "chr1"), IRanges(c(1, 20), c(10, 30)))
gr2 <- GRanges(c("chr1", "chr1"), IRanges(c(5, 25), c(6, 26)))
gr1
gr2
ov <- findOverlaps(gr1, gr2)
ov
queryHits(ov)
subjectHits(ov)
gnew <- pintersect(gr1[queryHits(ov)], gr2[subjectHits(ov)])
gnew
class(exome_regions)
seqnames(exome_regions)
seqnames(gene_annotations)
seqinfo(genes)
help("sub")
# Define new UCSC-style sequence levels
old <- seqlevels(exome_regions)
new <- sub("^MT$", "chrM", sub("^([0-9]+|X|Y)$", "chr\\1", old))
# Apply the renaming
names(new) <- old  # mapping from old to new
new
exome_regions <- renameSeqlevels(exome_regions, new)
seqnames(exome_regions)
seqnames(gene_annotations)
seqlevels(exome_regions)
seqlevels(gene_annotations)
# Sequence levels
exome_seqlevels <- seqlevels(exome_regions)
gene_seqlevels <- seqlevels(gene_annotations)
# Find sequence levels in exome_regions but not in gene_annotations
setdiff(exome_seqlevels, gene_seqlevels)
# Find sequence levels in gene_annotations but not in exome_regions
setdiff(gene_seqlevels, exome_seqlevels)
# Find the common sequence levels
intersect(exome_seqlevels, gene_seqlevels)
gene_seqlevels <- seqlevels(gene)
gene_seqlevels <- seqlevels(genes)
# Find sequence levels in exome_regions but not in gene_annotations
setdiff(exome_seqlevels, gene_seqlevels)
# Find sequence levels in gene_annotations but not in exome_regions
setdiff(gene_seqlevels, exome_seqlevels)
# Find the common sequence levels
intersect(exome_seqlevels, gene_seqlevels)
genes
length(gene_annotations) - length(genes)
length(gene_annotations)
length(exome_regions)
help("findOverlaps")
ov = findOverlaps(exome_regions, gene_annotations)
ov
ov1 <- findOverlaps(exome_regions, gene_annotations)
ov2 <- findOverlaps(exome_regions, genes)
ov2
hits <- findOverlaps(genes, exome_regions)
# Do we need pintersect the hits?
sequenced_genes <- genes[queryHits(hits)]
sequenced_genes
hits
sequenced_genes
length(genes)
length(exome_regions)
# Do we need pintersect the hits?
sequenced_genes <- genes[queryHits(hits)] %>% unique()
sequenced_genes
# Do we need pintersect the hits?
sequenced_genes <- genes[queryHits(hits)]
sequenced_genes
# Do we need pintersect the hits?
sequenced_genes <- genes[queryHits(hits)] %>% unique()
sequenced_genes
hits <- findOverlaps(gene_annotations, exome_regions)
hits
# Do we need pintersect the hits?
sequenced_genes <- genes[queryHits(hits)] %>% unique() # Keeping only one
sequenced_genes
# Do we need pintersect the hits?
sequenced_genes <- genes[queryHits(hits)] %>% unique() # Keeping only one
hits
# Do we need pintersect the hits?
sequenced_genes <- genes[queryHits(hits)] %>% unique() # Keeping only one
# Do we need pintersect the hits?
sequenced_genes <- gene_annotations[queryHits(hits)] %>% unique() # Keeping only one
sequenced_genes
BiocManager::install("maftools")
# 3. Read the MAF file for mutated genes
maf <- read.maf(maf = "../data/mc3.maf")
library(maftools)
# 3. Read the MAF file for mutated genes
maf <- read.maf(maf = "../data/mc3.maf")
length(maf)
maf
# Get mutated genes for maf file
mutated_genes <- getGeneSummary(maf)$Hugo_Symbol
mutated_genes
gene_names <- mcols(sequenced_genes)$gene_name  # or "gene_id" depending on the GTF
gene_names
# Get mutated genes for maf file
mutated_genes_names <- getGeneSummary(maf)$Hugo_Symbol
mutated_genes_names
# Get mutated genes for maf file
mutated_genes_names <- getGeneSummary(maf)$Hugo_Symbol %>% unique()
mutated_genes_names
# Genes that were sequenced but not mutated
zero_mutation_genes <- sequenced_genes[!(gene_names %in% mutated_gene_names)]
# Do we need pintersect the hits?
sequenced_genes <- gene_annotations[queryHits(hits)] %>% unique() # Keeping only one individual
gene_names <- mcols(sequenced_genes)$gene_name  # or "gene_id" depending on the GTF
# Genes that were sequenced but not mutated
zero_mutation_genes <- sequenced_genes[!(gene_names %in% mutated_gene_names)]
zero_mutation_genes
# Get mutated genes for maf file
mutated_genes_names <- getGeneSummary(maf)$Hugo_Symbol %>% unique()
mutated_genes_names
gene_names <- mcols(sequenced_genes)$gene_name  # or "gene_id" depending on the GTF
# Do we need pintersect the hits?
sequenced_genes <- gene_annotations[queryHits(hits)] %>% unique() # Keeping only one individual
# Genes that were sequenced but not mutated
zero_mutation_genes <- sequenced_genes[!(gene_names %in% mutated_gene_names)]
# Get mutated genes for maf file
mutated_gene_names <- getGeneSummary(maf)$Hugo_Symbol %>% unique()
mutated_gene_names
# Do we need pintersect the hits?
sequenced_genes <- gene_annotations[queryHits(hits)] %>% unique() # Keeping only one individual
gene_names <- mcols(sequenced_genes)$gene_name  # or "gene_id" depending on the GTF
# Genes that were sequenced but not mutated
zero_mutation_genes <- sequenced_genes[!(gene_names %in% mutated_gene_names)]
zero_mutation_genes
zero_mutation_gene_names <- mcols(zero_mutation_genes)$gene_name
zero_mutation_gene_names
zero_mutation_gene_names <- mcols(zero_mutation_genes)$gene_name %>% unique()
zero_mutation_gene_names
# Save a csv file for this
# Convert to data frame with one column named "gene_name"
df <- data.frame(hugo_symbol = zero_mutation_gene_names)
# Write to CSV (no row names)
write.csv(df, file = "../data/zero_mutation_genes.csv", row.names = FALSE)
