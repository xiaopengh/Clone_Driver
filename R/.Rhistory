length(unique(gnew@elementMetadata@listData[["gene_name"]]))
gene_names <- mcols(gnew)$gene_name
# Genes that were sequenced but not mutated
zero_mutation_genes <- sequenced_genes[!(gene_names %in% mutated_gene_names)]
gene_names <- mcols(gnew)$gene_name %>% unique()
library(dplyr)
gene_names <- mcols(gnew)$gene_name %>% unique()
length(gene_names)
length(mutated_gene_names)
# Genes that were sequenced but not mutated
zero_mutation_genes <- sequenced_genes[!(gene_names %in% mutated_gene_names)]
zero_mutation_gene_names <- mcols(zero_mutation_genes)$gene_name %>% unique()
gene_names <- mcols(gnew)$gene_name %>% unique()
# Get the gene_names that doesn't appear in maf file
zero_mutation_gene_names <- gene_names[!(gene_names %in% mutated_gene_names)]
# Save a csv file for this
# Convert to data frame with one column named "hugo_symbol"
df <- data.frame(hugo_symbol = zero_mutation_gene_names)
# Write to CSV (no row names)
write.csv(df, file = "../data/zero_mutation_genes.csv", row.names = FALSE)
mutated_gene_names
rm(maf)
View(gene_annotations)
View(gene_annotations)
gene_annotations$type
gene_annotations$type %>% unique()
# 1. Tabulate counts
tab_gene_type <- table(gene_annotations$type)
# 2. Basic barplot
barplot(tab,
main    = "Counts of annotation types",
xlab    = "Level",
ylab    = "Frequency",
las     = 1,        # make axis labels horizontal
col     = "steelblue",
border  = "white")
# 2. Basic barplot
barplot(tab_gene_type,
main    = "Counts of annotation types",
xlab    = "Level",
ylab    = "Frequency",
las     = 1,        # make axis labels horizontal
col     = "steelblue",
border  = "white")
# Extract only individuals with type "gene"
genes <- gene_annotations[gene_annotations$type == "gene"]
# ==============================================================================
# This only extract hugo symbol of individuals with type 'gene' in gtf
# ==============================================================================
# Find overlaps keyword any
hits <- findOverlaps(genes, exome_regions)
hits
# Get sequenced genes during the experiment
sequenced_genes <- gene_annotations[queryHits(hits)]
# Get sequenced genes during the experiment
sequenced_genes <- genes[queryHits(hits)]
# Get sequenced genes during the experiment
sequenced_genes <- genes[queryHits(hits)]
exome_hit <- exome_regions[subjectHits(hits)]
gnew <- pintersect(sequenced_genes, exome_hit)
gene_names <- mcols(gnew)$gene_name %>% unique()
# Get the gene_names that doesn't appear in maf file
zero_mutation_gene_names <- gene_names[!(gene_names %in% mutated_gene_names)]
length(gene_names)
View(gr1)
mutated_gene_names
# ==============================================================================
# This extract hugo symbols for all feature types(gene, exon, etc) in gtf file
# ==============================================================================
# Find overlaps keyword any
hits <- findOverlaps(gene_annotations, exome_regions)
length(hits)
# Get sequenced genes during the experiment
sequenced_genes <- gene_annotations[queryHits(hits)]
exome_hit <- exome_regions[subjectHits(hits)]
gnew <- pintersect(sequenced_genes, exome_hit)
View(gnew)
gene_names <- mcols(gnew)$gene_name %>% unique()
length(gene_names)
# Get the gene_names that doesn't appear in maf file
zero_mutation_gene_names <- gene_names[!(gene_names %in% mutated_gene_names)]
length(zero_mutation_gene_names)
# ==============================================================================
# This only extract hugo symbol of individuals with type 'gene' in gtf
# ==============================================================================
# Find overlaps keyword any
hits <- findOverlaps(genes, exome_regions)
length(hits)
# Get sequenced genes during the experiment
sequenced_genes <- genes[queryHits(hits)]
exome_hit <- exome_regions[subjectHits(hits)]
gnew <- pintersect(sequenced_genes, exome_hit)
gene_names <- mcols(gnew)$gene_name %>% unique()
length(gene_names)
# Get the gene_names that doesn't appear in maf file
zero_mutation_gene_names <- gene_names[!(gene_names %in% mutated_gene_names)]
# Save a csv file for this
# Convert to data frame with one column named "hugo_symbol"
df <- data.frame(hugo_symbol = zero_mutation_gene_names)
# Write to CSV (no row names)
write.csv(df, file = "../data/zero_mutation_genes.csv", row.names = FALSE)
gc()
library(GenomicRanges)
library(rtracklayer)
library(maftools)
library(dplyr)
# 1. Read the BED file of sequenced regions
exome_regions <- import("../data/gencode.v19.basic.exome.bed")
# 2. Read the GTF file of gene annotations
gene_annotations <- import("../data/gencode.v19.annotation.gtf")
# Count table
tab_gene_type <- table(gene_annotations$type)
# Basic barplot
barplot(tab_gene_type,
main    = "Counts of annotation types",
xlab    = "Level",
ylab    = "Frequency",
las     = 1,        # make axis labels horizontal
col     = "steelblue",
border  = "white")
# Extract only individuals with type "gene"
genes <- gene_annotations[gene_annotations$type == "gene"]
# 3. Read the MAF file for mutated genes
maf <- read.maf(maf = "../data/mc3.maf")
# Get mutated genes for maf file
mutated_gene_names <- getGeneSummary(maf)$Hugo_Symbol
mutated_gene_names
rm(maf)
# Define new UCSC-style sequence levels
# ==============================================================================
# Basically it changes 1 to chr1
old <- seqlevels(exome_regions)
new <- sub("^MT$", "chrM", sub("^([0-9]+|X|Y)$", "chr\\1", old))
# Apply the renaming
names(new) <- old  # mapping from old to new
exome_regions <- renameSeqlevels(exome_regions, new)
View(exome_regions)
View(gene_annotations)
View(exome_regions)
# Sequence levels
exome_seqlevels <- seqlevels(exome_regions)
gene_seqlevels <- seqlevels(gene_annotations)
# Find sequence levels in exome_regions but not in gene_annotations
setdiff(exome_seqlevels, gene_seqlevels)
# Find sequence levels in gene_annotations but not in exome_regions
setdiff(gene_seqlevels, exome_seqlevels)
# Find the common sequence levels
intersect(exome_seqlevels, gene_seqlevels)
rm(exome_seqlevels, gene_seqlevels)
# ==============================================================================
# This extract hugo symbols for all feature types(gene, exon, etc) in gtf file
# ==============================================================================
# Find overlaps keyword any
hits <- findOverlaps(gene_annotations, exome_regions)
length(hits)
# Get sequenced genes during the experiment
sequenced_genes <- gene_annotations[queryHits(hits)]
exome_hit <- exome_regions[subjectHits(hits)]
gnew <- pintersect(sequenced_genes, exome_hit)
gene_names <- mcols(gnew)$gene_name # %>% unique()
length(gene_names)
# Get the gene_names that doesn't appear in maf file
zero_mutation_gene_names <- gene_names[!(gene_names %in% mutated_gene_names)]
length(zero_mutation_gene_names)
gene_names <- mcols(gnew)$gene_name %>% unique()
length(gene_names)
# Get the gene_names that doesn't appear in maf file
zero_mutation_gene_names <- gene_names[!(gene_names %in% mutated_gene_names)]
length(zero_mutation_gene_names)
# ==============================================================================
# This only extract hugo symbol of individuals with type 'gene' in gtf
# ==============================================================================
# Find overlaps keyword any
hits <- findOverlaps(genes, exome_regions)
length(hits)
# Get sequenced genes during the experiment
sequenced_genes <- genes[queryHits(hits)]
exome_hit <- exome_regions[subjectHits(hits)]
gnew <- pintersect(sequenced_genes, exome_hit)
gene_names <- mcols(gnew)$gene_name %>% unique()
length(gene_names)
# Get the gene_names that doesn't appear in maf file
zero_mutation_gene_names <- gene_names[!(gene_names %in% mutated_gene_names)]
length(zero_mutation_gene_names)
# ==============================================================================
# This extract hugo symbols for all feature types(gene, exon, etc) in gtf file
# ==============================================================================
# Find overlaps keyword any
hits <- findOverlaps(gene_annotations, exome_regions)
length(hits)
# Get sequenced genes during the experiment
sequenced_genes <- gene_annotations[queryHits(hits)]
exome_hit <- exome_regions[subjectHits(hits)]
gnew <- pintersect(sequenced_genes, exome_hit)
gene_names <- mcols(gnew)$gene_name %>% unique()
length(gene_names)
# 1. Read the BED file of sequenced regions
exome_regions <- import("../data/gencode.v19.basic.exome.bed")
library(GenomicRanges)
library(rtracklayer)
library(maftools)
library(dplyr)
# 1. Read the BED file of sequenced regions
exome_regions <- import("../data/gencode.v19.basic.exome.bed")
# 2. Read the GTF file of gene annotations
gene_annotations <- import("../data/gencode.v19.annotation.gtf")
# Count table
tab_gene_type <- table(gene_annotations$type)
# Basic barplot
barplot(tab_gene_type,
main    = "Counts of annotation types",
xlab    = "Level",
ylab    = "Frequency",
las     = 1,        # make axis labels horizontal
col     = "steelblue",
border  = "white")
# Extract only individuals with type "gene"
genes <- gene_annotations[gene_annotations$type == "gene"]
# 3. Read the MAF file for mutated genes
maf <- read.maf(maf = "../data/mc3.maf")
# Get mutated genes for maf file
mutated_gene_names <- getGeneSummary(maf)$Hugo_Symbol
mutated_gene_names
rm(maf)
# Define new UCSC-style sequence levels
# ==============================================================================
# Basically it changes 1 to chr1
old <- seqlevels(exome_regions)
new <- sub("^MT$", "chrM", sub("^([0-9]+|X|Y)$", "chr\\1", old))
# Apply the renaming
names(new) <- old  # mapping from old to new
exome_regions <- renameSeqlevels(exome_regions, new)
# Sequence levels verification
exome_seqlevels <- seqlevels(exome_regions)
gene_seqlevels <- seqlevels(gene_annotations)
setdiff(exome_seqlevels, gene_seqlevels) # expected 0
setdiff(gene_seqlevels, exome_seqlevels) # expected 0
intersect(exome_seqlevels, gene_seqlevels) # expected chr1 ... chrY
rm(exome_seqlevels, gene_seqlevels)
summary(gene_annotations)
?import
gene_annotations
exome_regions
# ==============================================================================
# This extract hugo symbols for all feature types(gene, exon, etc) in gtf file
# ==============================================================================
# Find overlaps keyword any
hits <- findOverlaps(gene_annotations, exome_regions)
length(hits)
# Get sequenced genes during the experiment
sequenced_genes <- gene_annotations[queryHits(hits)]
exome_hit <- exome_regions[subjectHits(hits)]
gnew <- pintersect(sequenced_genes, exome_hit)
gene_names <- mcols(gnew)$gene_name %>% unique()
gnew
sequenced_genes[1]
exome_hit[1]
gnew[1]
View(gene_annotations)
gene_annotations@elementMetadata@listData[["gene_type"]]
# 3. Read the MAF file for mutated genes
maf_data <- fread(
input = "../data/mc3.maf",
sep = "\t",                # MAF is tab-delimited
header = TRUE,             # First non-comment line is the header
skip = "^#",               # Skip all lines starting with '#'
fill = TRUE,               # Fill missing columns if any
quote = "",                # Disable quoting to avoid parsing errors
na.strings = c("NA", ".", "")  # Common NA representations
)
library(GenomicRanges)
library(rtracklayer)
library(maftools)
library(maftools)
library(data.table)
library(dplyr)
# 1. Read the BED file of sequenced regions
exome_regions <- import("../data/gencode.v19.basic.exome.bed")
# 2. Read the GTF file of gene annotations
gene_annotations <- import("../data/gencode.v19.annotation.gtf")
# Count table
tab <- table(gene_annotations$type)
# Basic barplot
barplot(tab,
las     = 2,        # make axis labels horizontal
col     = "steelblue",
border  = "white")
# Extract only individuals with type "gene"
genes <- gene_annotations[gene_annotations$type == "gene"]
# Barplot of 'gene_type' categories
tab <- table(genes$gene_type)
barplot(tab, col="lightgreen", las=2)
# Extract only individuals with type "gene"
# also filter by gene_type == "protein_coding" & level %in% c('1', '2')
protein_genes <- genes[genes$gene_type == "protein_coding" & genes$level %in% c('1', '2')]
# 3. Read the MAF file for mutated genes
maf_data <- fread(
input = "../data/mc3.maf",
sep = "\t",                # MAF is tab-delimited
header = TRUE,             # First non-comment line is the header
skip = "^#",               # Skip all lines starting with '#'
fill = TRUE,               # Fill missing columns if any
quote = "",                # Disable quoting to avoid parsing errors
na.strings = c("NA", ".", "")  # Common NA representations
)
# 3. Read the MAF file for mutated genes
maf_data <- fread(
input = "../data/mc3.maf",
sep = "\t",                # MAF is tab-delimited
header = TRUE,             # First non-comment line is the header
fill = TRUE,               # Fill missing columns if any
quote = "",                # Disable quoting to avoid parsing errors
na.strings = c("NA", ".", "")  # Common NA representations
)
View(maf_data)
str(maf_data)
mutated_gene_names <- unique(maf_data$Hugo_Symbol)
mutated_gene_names
# Define new UCSC-style sequence levels
# ==============================================================================
# Basically it changes 1 to chr1
old <- seqlevels(exome_regions)
new <- sub("^MT$", "chrM", sub("^([0-9]+|X|Y)$", "chr\\1", old))
# Apply the renaming
names(new) <- old  # mapping from old to new
exome_regions <- renameSeqlevels(exome_regions, new)
# Sequence levels verification
exome_seqlevels <- seqlevels(exome_regions)
gene_seqlevels <- seqlevels(gene_annotations)
setdiff(exome_seqlevels, gene_seqlevels) # expected 0
setdiff(gene_seqlevels, exome_seqlevels) # expected 0
intersect(exome_seqlevels, gene_seqlevels) # expected chr1 ... chrY
rm(exome_seqlevels, gene_seqlevels)
rm(old, new)
gene_annotations$gene_name %>% str()
gene_annotations$gene_name %>% seqlevels()
tab <- table(gene_annotations$gene_name)
tab
barplot(tab)
# ==============================================================================
# This extract hugo symbols for all feature types(gene, exon, etc) in gtf file
# ==============================================================================
# Find overlaps keyword any
hits <- findOverlaps(gene_annotations, exome_regions)
length(hits)
# Get sequenced genes during the experiment
sequenced_genes <- gene_annotations[queryHits(hits)]
exome_hit <- exome_regions[subjectHits(hits)]
# ==============================================================================
# This extract hugo symbols for all feature types(gene, exon, etc) in gtf file
# ==============================================================================
# Find overlaps keyword any
hits <- findOverlaps(gene_annotations, exome_regions)
length(hits)
# Get sequenced genes during the experiment
sequenced_genes <- gene_annotations[queryHits(hits)]
exome_hit <- exome_regions[subjectHits(hits)]
gnew <- pintersect(sequenced_genes, exome_hit)
gene_names <- mcols(gnew)$gene_name %>% unique()
length(gene_names)
# Get the gene_names that doesn't appear in maf file
zero_mutation_gene_names <- gene_names[!(gene_names %in% mutated_gene_names)]
length(zero_mutation_gene_names)
# ==============================================================================
# This only extract hugo symbol of individuals with type 'gene' in gtf
# ==============================================================================
# Find overlaps keyword any
hits <- findOverlaps(genes, exome_regions)
length(hits)
# Get sequenced genes during the experiment
sequenced_genes <- genes[queryHits(hits)]
exome_hit <- exome_regions[subjectHits(hits)]
gnew <- pintersect(sequenced_genes, exome_hit)
gene_names <- mcols(gnew)$gene_name %>% unique()
length(gene_names)
# Get the gene_names that doesn't appear in maf file
zero_mutation_gene_names <- gene_names[!(gene_names %in% mutated_gene_names)]
length(zero_mutation_gene_names)
# ==============================================================================
# extract hugo symbol of individuals with type 'gene' in gtf
# + filtered by gene_type == "protein_coding" & level %in% c('1', '2')
# ==============================================================================
# Find overlaps keyword any
hits <- findOverlaps(protein_genes, exome_regions)
length(hits)
# Get sequenced genes during the experiment
sequenced_genes <- protein_genes[queryHits(hits)]
exome_hit <- exome_regions[subjectHits(hits)]
gnew <- pintersect(sequenced_genes, exome_hit)
gene_names <- mcols(gnew)$gene_name %>% unique()
length(gene_names)
# Get the gene_names that doesn't appear in maf file
zero_mutation_gene_names <- gene_names[!(gene_names %in% mutated_gene_names)]
length(zero_mutation_gene_names)
# Save a csv file for this
# Convert to data frame with one column named "hugo_symbol"
df <- data.frame(hugo_symbol = zero_mutation_gene_names)
# Write to CSV (no row names)
write.csv(df, file = "../data/zero_mutation_genes.csv", row.names = FALSE)
library(GenomicRanges)
library(rtracklayer)
library(ggplot2)
library(dplyr)
library(tidyr)
library(ggpubr)
library(circlize)
library(ggbio)
library(GenomeInfoDb)
library(RColorBrewer)
library(viridis)
getwd()
# 1. Distribution of gene types
gene_type_dist <- ggplot(as.data.frame(genes), aes(x = gene_type)) +
geom_bar(fill = "steelblue") +
theme_minimal() +
theme(axis.text.x = element_text(angle = 45, hjust = 1)) +
labs(title = "Distribution of Gene Types",
x = "Gene Type",
y = "Count")
gene_type_dist
library(GenomicRanges)
library(rtracklayer)
library(ggplot2)
library(dplyr)
library(tidyr)
library(ggpubr)
library(circlize)
library(ggbio)
library(GenomeInfoDb)
library(RColorBrewer)
library(viridis)
getwd()
?rtracklayer
??rtracklayer
?import
gene_annotations <- import("../data/gencode.v48.basic.annotation.gtf")
library(GenomicRanges)
library(rtracklayer)
library(data.table)
library(dplyr)
?import
gene_annotations <- import("../data/gencode.v48.basic.annotation.gtf")
View(gene_annotations)
gene_annotations@elementMetadata@listData[["source"]]
gene_annotations <- import("../data/gencode.v48.basic.annotation.gtf")
View(gene_annotations)
gene_annotations[1]
gene_annotations$level
gene_annotations$type
View(gene_annotations)
?slot
methods::getSlots("GRanges")
?granges
library(GenomicFeatures)
library(Biostrings)
library(GenomicRanges)
library(rtracklayer)
?makeTxDbFromGFF
library(txdbmaker)
# ---- STEP 1: Load GTF and extract CDS regions ----
txdb <- makeTxDbFromGFF("../data/gencode.v48.annotation.gtf", format = "gtf")
txdb
txdb[1]
dbconn(txdb)  # Gets the underlying SQLite connection
dbListTables(dbconn(txdb))
dbListTables(dbconn(txdb))
dbGetQuery(dbconn(txdb), "SELECT * FROM transcript LIMIT 5")
library(DBI)
dbGetQuery(dbconn(txdb), "SELECT * FROM transcript LIMIT 5")
conn <- dbconn(txdb)
dbListTables()
dbListTables(conn)
??DBI
??DBI
?cdsBy
browseVignettes("ggbio")
cds_by_tx <- cdsBy(txdb, by = "tx", use.names = TRUE)
View(cds_by_tx)
cds_by_tx[1]
# ---- Load transcript FASTA ----
tx <- readDNAStringSet("../data/gencode.v48.transcripts.fa")
View(tx)
library(GenomicFeatures)
library(Biostrings)
library(GenomicRanges)
library(rtracklayer)
library(txdbmaker)
# ---- Load GTF and extract CDS regions ----
txdb <- makeTxDbFromGFF("../data/gencode.v48.annotation.gtf", format = "gtf")
cds_by_tx <- cdsBy(txdb, by = "tx", use.names = TRUE)
# ---- Load transcript FASTA ----
genome <- readDNAStringSet("../data/gencode.v48.transcripts.fa", format = "fasta")
genome[1]
length(genome[1])
length(genome[1]$seq)
# Visualize the first few transcripts
tx <- head(genome, 5)
View(tx)
tx
?DNAStringSet
names(tx)
library(GenomicFeatures)
library(Biostrings)
library(GenomicRanges)
library(rtracklayer)
library(txdbmaker)
# ---- Load GTF and extract CDS regions ----
txdb <- makeTxDbFromGFF("../data/gencode.v48.annotation.gtf", format = "gtf")
cds_by_tx <- cdsBy(txdb, by = "tx", use.names = TRUE)
# ---- Load transcript FASTA ----
genome <- readDNAStringSet("../data/gencode.v48.transcripts.fa", format = "fasta")
save.image("~/Desktop/Clone_Driver/R/.RData")
